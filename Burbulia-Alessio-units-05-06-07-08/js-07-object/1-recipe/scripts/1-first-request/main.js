/**
 * @file: main.js
 * @author: Alessio Burbulia
 * 
 * 1.Recipe
 *  ● Create an object to hold information on your favorite recipe. It should have
 *    properties for title (a string), servings (a number), and ingredients (an
 *    array of strings).
 *  ● On separate lines (one console.log statement for each), log the recipe
 *    information
 * 
 *  Bonus: Create an array that holds several recipes and log them all
 */

// unit of measurement = grams [g] (solid)
const recipe = {
    title : `Classic Ragu`,
    servings : 4,
    ingredients : [
        fsPrepration = {
            preparationName : `sauteed`,
            ftIngredient : `cattors`,
            sdIngredient : `onion`,
            thIngredient : `oliveOil`
        },

        sdPreparation = {
            preparationName : `mixed minced meat`,
            ftIngredient : `beef`,
            sdIngredient : `pork meat`
        },

        `tomatoPuree`,
        `water`,
        `pepper`,
        `salt`
    ]
}

console.log(
    `Our recipe named ${recipe.title} maked for ${recipe.servings} peoples we need 
    1. ${recipe.ingredients[0].preparationName},
    2. ${recipe.ingredients[1].preparationName},
    3. ${recipe.ingredients[2]},
    4. ${recipe.ingredients[3]},
    5. ${recipe.ingredients[4]},
    6. ${recipe.ingredients[5]}`
);